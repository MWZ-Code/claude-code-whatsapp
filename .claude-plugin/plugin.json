{
  "name": "whatsapp",
  "description": "WhatsApp channel for Claude Code — long-running HTTP server (Baileys / WhatsApp Web Multi-Device) with built-in access control. Consumed via plain HTTP on loopback; the bundled whatsapp-channel skill documents the contract.",
  "version": "0.1.0",
  "keywords": [
    "whatsapp",
    "messaging",
    "channel",
    "http"
  ]
}
